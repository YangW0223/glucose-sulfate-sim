# Binary reference assets should be tracked via Git LFS when pushing from a local clone.
docs/reference/*.mp4 filter=lfs diff=lfs merge=lfs -text
docs/reference/*.docx filter=lfs diff=lfs merge=lfs -text
